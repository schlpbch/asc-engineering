---
import DSPageSection from '@asc-engineering/design-system/patterns/PageSection.astro';

interface Props {
  id?: string;
  title: string;
  subtitle?: string;
  border?: boolean;
  padding?: string;
}

const props = Astro.props;
---

<DSPageSection {...props}><slot /></DSPageSection>
